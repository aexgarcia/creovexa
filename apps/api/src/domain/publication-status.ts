/** Shared business vocabulary for per-destination delivery and campaign summaries. */
export enum PublicationStatus {
  PENDING = 'PENDING',
  PUBLISHING = 'PUBLISHING',
  PUBLISHED = 'PUBLISHED',
  FAILED = 'FAILED',
}
